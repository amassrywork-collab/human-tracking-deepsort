# Core
numpy>=1.24,<3.0
opencv-python>=4.8,<5.0
tqdm>=4.66,<5.0

# Detection (YOLO)
ultralytics>=8.3.0

# Tracking
# (Removed deep-sort-realtime as we will use native Ultralytics tracking)

# Optional but useful utilities
scipy>=1.10,<2.0
filterpy>=1.4,<2.0

# Web Server
fastapi>=0.100.0
uvicorn>=0.20.0
python-multipart
Flask>=2.0
flask-cloudflared>=0.0.15

# Torch note:
# Ultralytics will require PyTorch. Install it depending on your environment (CPU/GPU) using the official PyTorch command.
# Example (CPU only):
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
